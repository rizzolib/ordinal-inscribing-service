 - POST /api/text-estimate-fee/
    Request:
        content: [string](in bulk case, will be array)
        padding: number (330 ~ 546)
        feeRate: number
    
    Response:
        networkFee: number,
        serviceFee: number,
        feeBySize: number
        total: number

- POST /api/file-estimate-fee/
    Request:
        content: buffer
        padding: number (330 ~ 546)
        feeRate: number
    Response:
        networkFee: number,
        serviceFee: number,
        feeBySize: number
        total: number
